#!/usr/bin/env bash
# This script stops the '4-to_infinity_and_beyond' process without using 'kill' or 'killall'.

# pkill -f: sends a SIGTERM (default signal) to processes matching the pattern.
# This is a common utility for stopping processes by name/pattern.
pkill -f "4-to_infinity_and_beyond"
